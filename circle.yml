machine:
  node:
    version: 7.0.0

  timezone:
    America/Denver

  test:
    override:
      - npm run test

  deployment:
    staging:
      branch: staging
      heroku:
        # appname: staging branch name in heroku

  production:
    branch: productionheroku:
      # appname: production branch name in heroku
